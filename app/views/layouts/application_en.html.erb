<!DOCTYPE html>
<html>
  <head>
    <title>3D Print Shop</title>
    <link href="https://fonts.googleapis.com/css?family=Roboto+Condensed" rel="stylesheet">
    <link rel="shortcut icon" type="image/x-icon" href="<%= image_url("favicon.ico") %>" />
    <%= csrf_meta_tags %>
    <%= stylesheet_link_tag    'application', media: 'all', 'data-turbolinks-track': 'reload' %>
    <%= javascript_include_tag 'application', 'data-turbolinks-track': 'reload' %>
    <%= javascript_pack_tag 'application' %>
    <%= javascript_pack_tag 'promptpayqr' %>
    <%= javascript_pack_tag 'preview' %>
  </head>
  <body>
    <div class="content">
      <div class="page-header">
        <div class="navbar">
          <div class="navbar-left">
            <a style="font-size:200%;" href="/">3D Print Shop</a>
            <a style="margin-left:1rem;" href="/">Tutorials</a>
            <p style="display:inline; margin:0 1rem 0 1rem;">|</p>
            <a href="/">Pricing</a>
            <p style="display:inline; margin:0 1rem 0 1rem;">|</p>
            <a href="/">Contact Us</a>
          </div>
          <div class="navbar-right">
            <% if current_user %>
              <b style="color:#3F51B5; padding-right:1rem;">Hello, <%= current_user.username %></b>
              <a href="/my_cart">Shopping Cart</a>
              <div id="dropdown-menu">
                <span style="font-size:150%; padding-left:1rem;">☰</span>
                <div id="dropdown-container">
                    <% if is_customer? %>
                        <a href="#">My account</a>
                        <a href="/my_models">My uploads</a>
                    <% end %>
                    <a href="/logout">Log Out</a>
                </div>
              </div>
            <% else %>
              <a style="margin-right:1rem;" href="/login">Log In</a>
              <a href="/signup">Sign Up</a>
            <% end %>
          </div>
        </div>
      </div>
      <div class="page-content-container">
        <%= yield %>
      </div>
    </div>
    <div class="footer">
      <div class="footer-content">
        <canvas id="canvas"></canvas>
      </div>
    </div>
  </body>
</html>
